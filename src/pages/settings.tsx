import React, { useEffect, useRef, useState } from "react";
import { useRouter } from "next/router"
...
card"
      className="min-h-screen bg-background dot-grid"
    >
      ...
    </main>
  );
}

export default withAuth(SettingsPage);